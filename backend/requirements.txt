librosa>=0.10.0
numpy>=1.26.0
scipy>=1.11.0
soundfile>=0.12.1
sounddevice>=0.4.6
fastapi>=0.104.1
uvicorn>=0.24.0
python-multipart>=0.0.6
websockets>=12.0

# For video and audio processing
moviepy

# For AI-powered transcription
# Note: The first time you run the transcription, Whisper will download its model files.
# You can choose different model sizes (tiny, base, small, medium, large).
# The 'base' model is a good starting point.
openai-whisper

# The whisper library depends on torch for deep learning operations.
torch

# For progress bars during processing
tqdm